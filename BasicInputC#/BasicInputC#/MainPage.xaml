<Page x:Class="BasicInputC_.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:BasicInputC_"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
      Height="628"
      Width="649">

    <Grid Margin="40" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBox x:Name="UserInput"
                 Header="Enter your text " Grid.Row="0"
                 HorizontalAlignment="Left"
                 Margin="0,0,0,20"
                 Width="559" />
        
        <Button Content="Apply" Grid.Row="1"
                HorizontalAlignment="Stretch"
                Click="Button_Click"/>

        <TextBlock x:Name="TextOutput" 
                   Text="You will see things" Grid.Row="2"
                   HorizontalAlignment="Left" />
    </Grid>
</Page>
